name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - main
      - dev
      - feature/id-prefix-and-table-redesign
  pull_request:
    types: [opened, synchronize, reopened, closed]
    branches:
      - main
      - dev

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: true
          lfs: false

      # Node.js のセットアップ
      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20.x'

      # Freeプランでは「Bring your own API」がサポートされていないため、
      # APIの事前ビルドとアーティファクトのダウンロードは不要です
      # Static Web Appsが自動的にAPIをビルドします（Managed functionsとして）

      # Frontend の依存関係インストール
      - name: Install Frontend Dependencies
        working-directory: ./frontend/receiptfly-web
        run: npm ci

      # Frontend のビルド
      - name: Build Frontend
        working-directory: ./frontend/receiptfly-web
        env:
          VITE_API_BASE_URL: "/api"
        run: npm run build

      # staticwebapp.config.json を dist フォルダにコピー
      - name: Copy staticwebapp.config.json to dist folder
        run: |
          cp frontend/receiptfly-web/staticwebapp.config.json frontend/receiptfly-web/dist/staticwebapp.config.json

      # Azure Static Web Apps へのデプロイ
      # Freeプランでは「Bring your own API」がサポートされていないため、
      # skip_api_build: false にして、Static Web AppsにAPIをビルドさせる必要があります
      # これにより、APIは「Managed functions」として扱われます
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          repo_token: ${{ secrets.GITHUB_TOKEN }}
          action: "upload"
          ###### Repository/Build Configurations ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "frontend/receiptfly-web" # App source code path relative to repository root
          api_location: "backend/Receiptfly.Functions" # Api source code path relative to repository root - Static Web Appsがビルドします
          output_location: "dist" # Built app content directory, relative to app_location
          skip_api_build: false # FreeプランではManaged functionsとしてビルドする必要があります
          ###### End of Repository/Build Configurations ######

  close_pull_request_job:
    if: github.event_name == 'pull_request' && github.event.action == 'closed'
    runs-on: ubuntu-latest
    name: Close Pull Request Job
    steps:
      - name: Close Pull Request
        id: closepullrequest
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN }}
          action: "close"

